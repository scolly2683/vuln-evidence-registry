name: Tests

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - run: pip install -e ".[test,routing]"
      - name: Registry structural validation
        run: python -m routing_registry validate --registry registry
      - name: All tests (incl. routing regression fixtures — the anti-decay gate)
        run: pytest -q
